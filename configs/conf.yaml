defaults:
  - _self_
  - compute: local # must be slurm or local
  - exp: ???

base_output_dir: /scratch/pt2310/llvm-project/DiffusionModels #<YOUR EXPERIMENT FOLDER>
experiment: ???
sub_dir: ${experiment}
name: ???

experiment_dir: ???

hydra:
  job:
    name: ${name}
  callbacks:
    log_job_return_callback:
      _target_: submitit_hydra.LogJobReturnCallback
    
